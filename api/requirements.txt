# Runtime
fastapi==0.115.0
uvicorn[standard]==0.30.0
asyncpg==0.29.0
# psycopg2-binary e pandas sairam em 09/09/2026: nenhum arquivo .py do
# projeto importa qualquer um dos dois desde a remocao do pipeline de
# XLSX do CROmie. Juntos eram 34MB dos 94MB baixados pelo CI a cada
# invalidacao de cache. O acesso ao banco e 100% asyncpg.
# openpyxl fica: scripts/dados/gerar_payloads_oraculus.py ainda usa.
openpyxl==3.1.2
# Proposta comercial: preenche api/templates/proposta_modelo.pptx.
# ATENCAO: o deploy faz rsync e reinicia, NAO roda pip install. Instalar na
# EC2 a mao antes do primeiro uso:  sudo -iu hipo pip install python-pptx==1.0.2
# O PDF depende do LibreOffice, que e pacote do sistema:
#   sudo dnf install -y libreoffice-impress
python-pptx==1.0.2
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
pydantic==2.8.0
pydantic-settings==2.3.0
httpx==0.27.0
# 1.43.74 e a versao instalada a mao na EC2 em 19/08 e verificada contra o
# SES em producao. O deploy faz rsync e reinicia, NAO roda pip install:
# este pin so vale num provisionamento do zero, e precisa reproduzir o que
# esta rodando.
boto3==1.43.74
# Agenda: cria o evento no Google Calendar do anfitriao por conta de
# servico com delegacao em todo o dominio.
# ATENCAO, MESMA ARMADILHA DO python-pptx ACIMA: o deploy faz rsync e
# reinicia, NAO roda pip install. Instalar na EC2 a mao antes do primeiro
# uso:
#   sudo -iu hipo pip install google-api-python-client==2.149.0 google-auth==2.35.0
# Sem elas a API sobe igual e a agenda funciona igual -- so nao manda o
# convite, e diz isso na tela (services/google_agenda.problemas()).
google-api-python-client==2.149.0
google-auth==2.35.0

# Tests
anyio[trio]==4.4.0
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0